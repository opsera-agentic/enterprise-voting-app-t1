################################################################################
# ArgoCD Application - Hub-Spoke Model
# Generated by Opsera Code-to-Cloud v0.6
#
# Architecture:
#   Hub Cluster: argocd-usw2 (runs ArgoCD)
#   Spoke Cluster: opsera-usw2-np (runs workloads)
#
# The destination.name references a cluster registered with ArgoCD.
# Run: argocd cluster list  to see registered clusters
################################################################################

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: voting2-dev
  namespace: argocd
  labels:
    app.kubernetes.io/name: voting2
    app.kubernetes.io/instance: voting2-dev
    app.kubernetes.io/managed-by: argocd
    environment: dev
    tenant: opsera
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    # Notifications (optional - enable if argocd-notifications is installed)
    # notifications.argoproj.io/subscribe.on-sync-succeeded.slack: deployments
    # notifications.argoproj.io/subscribe.on-sync-failed.slack: deployments
spec:
  project: default

  source:
    repoURL: https://github.com/opsera-ai/enterprise-voting-app-t1.git
    targetRevision: main
    path: .opsera-voting2/k8s/overlays/dev

  # Hub-Spoke: Deploy to spoke cluster by name
  # The cluster must be registered in ArgoCD: argocd cluster add opsera-usw2-np
  # Alternative: Use 'server' with the cluster's API URL
  destination:
    # Option 1: By cluster name (requires cluster to be registered)
    name: opsera-usw2-np
    namespace: voting2-dev

    # Option 2: By server URL (uncomment if using direct URL)
    # server: https://XXXXXXXXX.gr7.us-west-2.eks.amazonaws.com
    # namespace: voting2-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ServerSideApply=true  # Recommended for large manifests
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  # Health checks
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas  # Allow HPA to control replicas

  revisionHistoryLimit: 10
